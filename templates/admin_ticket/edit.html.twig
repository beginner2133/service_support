{% extends 'base.html.twig' %}

{% block title %}Modifier Ticket #{{ ticket.id }} (Admin){% endblock %}

{% block body %}
<div class="content-card">
    <h1>Modifier le Ticket #{{ ticket.id }}</h1>

    {{ include('admin_ticket/_form.html.twig', {'button_label': 'Mettre à jour le ticket'}) }}

    <div class="mt-4 d-flex justify-content-between align-items-center">
        <a href="{{ path('app_admin_ticket_index') }}" class="btn btn-link">Retour à la liste des tickets</a>
        {{ include('admin_ticket/_delete_form.html.twig', {'button_label': 'Supprimer', 'button_class': 'btn btn-danger'}) }}
    </div>
</div>
{% endblock %}
