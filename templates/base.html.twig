<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Service Support Agence{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        
        {# Lien CSS Bootstrap via CDN #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
        
        <style>
            :root {
                --bleu-majorelle:rgb(74, 54, 225); 
                --bleu-majorelle-hover:rgba(8, 79, 185, 0.99);
                --text-nav-color: rgba(255,255,255,.85);
                --text-nav-hover-color: #ffffff;
                --body-bg-color: #f0f2f5; 
            }

            body { 
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
                margin: 0 0 60px 0; 
                background-color: var(--body-bg-color); 
                color: #333; 
                font-size: 16px; 
            }

            .main-nav { 
                background-color: var(--bleu-majorelle) !important; 
                padding: 0.8rem 1.2rem; 
                box-shadow: 0 2px 4px rgba(36, 36, 36, 0.2); 
            }
            .main-nav .navbar-brand { 
                font-size: 1.5em; 
                font-weight: bold; 
                color: var(--text-nav-hover-color); 
            }
            .main-nav .nav-link { 
                color: var(--text-nav-color); 
                font-size: 1.1em; 
                padding: 0.5rem 1rem; 
            }
            .main-nav .nav-link:hover,
            .main-nav .nav-link.active { 
                color: var(--text-nav-hover-color); 
                font-weight: 500;
            }
            .user-identifier-nav { 
                color: var(--text-nav-color); 
                margin-right: 15px; 
                font-size: 1em; 
                align-self: center;
            }
            .btn-logout-nav {
                color: var(--text-nav-color);
                border: 1px solid var(--text-nav-color);
                padding: 0.375rem 0.75rem;
                font-size: 1em;
            }
            .btn-logout-nav:hover {
                color: var(--bleu-majorelle);
                background-color: var(--text-nav-hover-color);
                border-color: var(--text-nav-hover-color);
            }

            .container-main { 
                margin-top: 2rem; 
                margin-bottom: 2rem;
                background-color: #ffffff; 
                padding: 2rem;
                border-radius: 8px;
                box-shadow: 0 0.125rem 0.25rem rgba(26, 26, 26, 0.07);
            }

            .content-card {
                background-color:rgb(244, 244, 244);
                padding: 2rem;
                border-radius: 0.5rem;
                box-shadow: 0 0.5rem 1rem rgba(23, 22, 22, 0.2);
                margin-bottom: 2rem;
            }
            .content-card h1, .content-card h2 {
                color: var(--bleu-majorelle);
                border-bottom: 2px solid #eee;
                padding-bottom: 0.5em;
                margin-bottom: 1em;
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% if app.user %} {# affiche ce menu seulement si un utilisateur est connecté #}
            <nav class="navbar navbar-expand-lg main-nav">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('app_home') }}">Support Agence</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon" style="background-image: url(\"data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%28255, 255, 255, 0.85%29' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e\");"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">Accueil Interne</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.attributes.get('_route') starts with 'app_ticket' %}active{% endif %}" href="{{ path('app_ticket_index') }}">Tickets</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_public_home') }}" target="_blank">Voir site public</a>
                            </li>
                        </ul>
                        <span class="navbar-text user-identifier-nav">
                            Connecté: {{ app.user.userIdentifier }}
                        </span>
                        <ul class="navbar-nav">
                             <li class="nav-item">
                                <a class="nav-link btn btn-sm btn-logout-nav" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        {% endif %}

        <div class="container-main container"> 
            {% block body %}{% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" xintegrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js" xintegrity="sha384-mQ93GR66B00ZXjt0YO5KlohRA5SY2XofN4zfuZxLkoj1gXtW8ANNCe9d5Y3eG5eD" crossorigin="anonymous"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
