{% extends 'base.html.twig' %}

{% block title %}Accueil Interne - Service Support Agence{% endblock %}

{% block body %}

<div class="content-card">
    <h1 class="mb-4">Tableau de Bord👋</h1>

    {% if message is defined and message is not empty %}
        <div class="alert alert-info">{{ message }}</div>
    {% endif %}

    <p class="lead">
        Bienvenue sur votre espace de gestion interne de l'Agence Web.
    </p>

    <p>
        Vous pouvez accéder aux différentes fonctionnalités de suivi des tickets clients.
    </p>
    <ul>
        <li>Consulter la <a href="{{ path('app_ticket_index') }}">liste des tickets</a>.</li>
        <li>Visualiser les détails de chaque ticket.</li>
        <li>Modifier le statut des tickets.</li>
        {% if is_granted('ROLE_ADMIN') %} {# liens spécifiques pour l'administrateur #}
            <li>Gérer les catégories de tickets.</li>
            <li>Gérer les statuts des tickets.</li>
            <li>Gérer les utilisateurs de l'agence.</li>
        {% endif %}
    </ul>

    <p>
        Nous vous souhaitons une bonne utilisation de cet outil.
    </p>

    {# informations sur l'utilisateur connecté #}
    {% if app.user %}
        <hr class="my-4">
        <p class="small text-muted">
            Vous êtes connecté en tant que : {{ app.user.userIdentifier }} (Rôles : {{ app.user.roles|join(', ') }})
        </p>
    {% endif %}
</div>
{% endblock %}
